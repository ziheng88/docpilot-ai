# NextAI

PDF question-answering web application with text/voice queries and spoken answers.

## Run locally

1. Copy server/.env.example to server/.env and provide an OpenAI API key.
2. In server/, run npm install.
3. In nextai/, run npm install and then npm run dev.

The client runs on port 3000 and the Express API runs on port 5001.
